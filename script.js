function stopBot(botId) {
  alert("Sending stop command for: " + botId);

  // In future, connect to backend API
  // fetch("https://your-cloudflare-link/api/stop_bot", {
  //   method: "POST",
  //   body: JSON.stringify({ botId }),
  // });
}